import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
    image: {
        borderRadius: 40,
        borderWidth: 1,
        borderColor: 'white',
    },
    container: {
        borderRadius: 40,
        margin: 8,
        borderWidth: 2,
        borderColor: 'black',
    }
})

export default Styles;
